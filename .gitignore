# İşletim sistemi dosyaları
.DS_Store
Thumbs.db

# Bağımlılıklar (En önemlisi bu)
/node_modules
package-lock.json

# Log (Günlük) dosyaları
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ortam değişkenleri dosyaları (Güvenlik için önemlidir)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build (Derleme) çıktıları
/build
/dist
node_modules
